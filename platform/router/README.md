# Router Layer

The router maps requests to roles based on policy rules and optional client hints.

Policy source of truth: `routing_policy.yaml`.

Implementation note:

- Current repository state includes policy only.
- Runtime router service is planned in the next implementation phase.
