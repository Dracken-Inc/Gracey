# Platform Layer

This directory contains the new Gracey runtime platform layers.

- `control/`: NemoClaw/OpenShell policy and lifecycle profile.
- `inference/`: role registry and runtime-specific profiles.
- `router/`: classification and routing policy for role selection.

All files here are scaffold-safe and can be validated without target hardware.
