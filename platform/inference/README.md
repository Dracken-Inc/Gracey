# Inference Layer

Inference in Gracey is role-based:

- fast
- heavy
- thinker
- architect

Role metadata is stored in `role_registry.yaml`.
Runtime profiles are separated under `runtimes/vllm` and `runtimes/trtllm`.
