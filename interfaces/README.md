# Interfaces Layer

This directory contains user-facing interface services.

- `api/`: FastAPI service contract and mock implementation.
- `agents/telegram/`: Telegram bridge scaffold.

Both interfaces are redesigned in the NemoClaw refactor and can run in mock mode.
