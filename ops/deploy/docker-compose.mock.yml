version: "3.9"

services:
  gracey-api:
    image: python:3.12-slim
    working_dir: /workspace/interfaces/api
    volumes:
      - ../../:/workspace
    command: >-
      bash -lc "pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0 --port 8080"
    ports:
      - "8080:8080"
