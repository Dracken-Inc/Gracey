# Legacy API Gateway Path

This path is kept for historical context from the OpenClaw scaffold.

The active API implementation scaffold is now located in:

- `interfaces/api/app/main.py`
- `interfaces/api/requirements.txt`

Primary stack settings now live in:

- `configs/gracey_stack.yaml`
- `platform/inference/role_registry.yaml`
- `platform/router/routing_policy.yaml`

See migration guide:

- `docs/migration_nemoclaw.md`
