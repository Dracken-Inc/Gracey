# service_config.yaml
# Deprecated compatibility config retained for legacy references.
#
# Primary stack configuration now lives in:
# - configs/gracey_stack.yaml
# - platform/inference/role_registry.yaml
# - platform/router/routing_policy.yaml
# - platform/control/nemoclaw_profile.yaml

meta:
  status: "deprecated"
  migration_doc: "docs/migration_nemoclaw.md"
  replacement_stack_config: "configs/gracey_stack.yaml"

compatibility:
  api_gateway:
    host: "0.0.0.0"
    port: 8080
    auth_token: "${API_GATEWAY_AUTH_TOKEN}"
    rate_limit_requests_per_minute: 60
  telegram:
    bot_token: "${TELEGRAM_BOT_TOKEN}"
    bot_username: "GraceyGB10Bot"
    api_url: "http://localhost:8080/v1/chat"

notes:
  - "Do not add new runtime settings here. Use configs/gracey_stack.yaml instead."
  - "Legacy OpenClaw values were removed during NemoClaw-first refactor."
